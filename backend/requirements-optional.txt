# Best-effort extras. Safe to skip entirely — the backend has built-in fallbacks.
#
# Python 3.14 (Windows): install forecasters only; CrewAI/litellm are skipped
# automatically (no cp314 wheels for tiktoken/crewai).
#   pip install -r requirements-optional.txt
#
# Python 3.10–3.13: full optional stack including CrewAI.
#   pip install -r requirements-optional.txt

# --- Forecasters (flag-gated; analytic fallback when absent) ---
chronos-forecasting>=1.4
prophet>=1.1
cmdstanpy>=1.2
# timesfm often lacks cp314 wheels; skip rather than fail the whole install.
timesfm[torch]>=1.3; python_version < "3.14"

# --- CrewAI report synthesis (CREWAI_ENABLED; default off) ---
# Requires Python <3.14 — litellm depends on tiktoken (Rust extension, no cp314 wheel).
crewai>=0.80; python_version < "3.14"
litellm>=1.40; python_version < "3.14"

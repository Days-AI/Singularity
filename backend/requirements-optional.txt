# Best-effort extras. These may fail to install on Python 3.14 / Windows; the
# backend runs fully without them (dependency-safe equivalents are built in).

# Probabilistic forecasting with native prediction intervals.
chronos-forecasting>=1.4

# Foundation + classical forecasters (replaces built-in Holt-Winters path when installed).
# timesfm may fail on Python 3.14; the backend falls back to Prophet-only or analytic.
timesfm[torch]>=1.3
prophet>=1.1
cmdstanpy>=1.2

# Full CrewAI Flow framework. The backend ships a lightweight async orchestrator
# that mirrors @start/@listen, so this is purely optional. When CREWAI_ENABLED is
# set and these are installed, the report stage is produced by a persona-injected
# crew (local Ollama generates, OpenRouter polishes); otherwise report.build()
# runs unchanged. crewai pulls in litellm, used to route to Ollama/OpenRouter.
crewai>=0.80
litellm>=1.40

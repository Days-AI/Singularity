# --- Core web / async ---
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
tenacity>=8.3

# --- Scientific stack (dependency-safe on py3.14 by 2026) ---
numpy>=1.26
pandas>=2.2
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.4

# --- Personality engine (Days234) ---
huggingface-hub>=0.24
transformers>=4.40
# torch is expected to be present (CPU build already installed system-wide).
# If your venv lacks it, install the CPU wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- Evidence agents ---
ddgs>=6.0
yfinance>=0.2
wikipedia>=1.4
arxiv>=2.1
# GDELT global news/event search (flag-gated; GDELT_ENABLED). Keyless; reuses
# pandas above. Import-guarded in tools.sources so the backend still boots if absent.
gdeltdoc>=1.5

# --- LangChain tool + Graph-RAG layer (flag-gated; LANGCHAIN_ENABLED / RAG_ENABLED) ---
# Import-guarded everywhere: the backend still boots if these are absent.
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3
langchain-huggingface>=0.1
sentence-transformers>=3.0
# Native LangChain search providers (flag-gated; LANGCHAIN_NATIVE_TOOLS).
# DuckDuckGo + Serper wrappers ship in langchain-community above; Parallel
# search/extract/chat ship in langchain-parallel. Import-guarded in
# tools.search_providers, so the backend still boots if this is absent.
langchain-parallel>=0.3

# --- Persistence (optional at runtime) ---
supabase>=2.5

# NOTE: Report/document generation (PDF, Word, charts) is handled entirely by
# the in-browser JavaScript stack in the frontend (jsPDF, docx, Plotly, etc.).
# The Python document-generation deps (reportlab, python-docx, matplotlib,
# jinja2) were removed; pandas/numpy above remain for data + analytics only.
